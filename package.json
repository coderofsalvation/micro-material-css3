{
  "name": "sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "builddev": "rm css/app.scss; cat css/*.scss > css/app.scss && node-sass --include_path css/bourbon --follow css/app.scss css/app.css",
    "build": "rm css/app.scss; cat css/*.scss > css/app.scss && node-sass --follow --output-style compressed css/app.scss css/app.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bourbon": "^4.2.7"
  }
}
